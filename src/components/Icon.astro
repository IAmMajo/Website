---
interface Props {
  name: string;
}

const { name } = Astro.props;

const lightIcon = (await import(`../icons/light/${name}.svg?raw`)).default;
const darkIcon = (await import(`../icons/dark/${name}.svg?raw`)).default;
---

<div class="light" set:html={lightIcon} />
<div class="dark" set:html={darkIcon} />

<style>
  div {
    height: 24px;
  }
  div :global(svg) {
    fill: currentColor;
  }
</style>
